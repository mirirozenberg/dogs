@model Dogs2.Models.QueueModel
@{
    ViewData["Title"] = "Details";
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
}

<div id="privacy" class="modal fade" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal modal-header">
                <h4 class="modal-title">Details</h4>
                @*<small class="font-weight-bold"></small>*@
            </div> <form role="form" method="post">
                <div class="modal-body">

                    <h2> @Html.DisplayFor(model => model.users1.displayName)</h2>


                    <div class="form-group">
                        Insert DateTime:  @Html.DisplayFor(model => model.insertDateTime)

                    </div>
                    <div class="form-group">
                        Queue DateTime:   @Html.DisplayFor(model => model.queueDateTime)
                    </div>

                </div>
                <div class="modal-footer">
                    <div style="width:100px">
                        <button asp-controller="Home" asp-action="Index" class="btn btn-info btn-block">Close</button>
                        <input name="__RequestVerificationToken" type="hidden" value="<removed for brevity>">
                    </div>
                    @*<button type="button" class="btn btn-primary" style="color:white"><span style="color:white">@Html.ActionLink("Colse", "Index")</span> </button>*@
                </div>
            </form>
        </div>
    </div>
</div>

@{
    <script>
        $(function () {
            $("#privacy").modal();
        });
    </script>
}

@*<div id='myModal' class='modal'>
        <div class="modal-dialog">
            <div class="modal-content">
                <div id='myModalContent'></div>
            </div>
        </div>

    </div>*@

@*@section scripts
    {
        <script src="~/Scripts/jquery-3.4.1.min.js"></script>
        <script src="~/Scripts/bootstrap.js"></script>
        <script src="~/Scripts/bootstrap.min.js"></script>
        <script>

        var TeamDetailPostBackURL = '/Home/Details';
        $(function () {
            $(".anchorDetail").click(function () {
                debugger;
                var $buttonClicked = $(this);
                var id = $buttonClicked.attr('data-id');
                var options = { "backdrop": "static", keyboard: true };
                $.ajax({
                    type: "GET",
                    url: TeamDetailPostBackURL,
                    contentType: "application/json; charset=utf-8",
                    data: { "Id": id },
                    datatype: "json",
                    success: function (data) {
                        debugger;
                        $('#myModalContent').html(data);
                        $('#myModal').modal(options);
                        $('#myModal').modal('show');

                    },
                    error: function () {
                        alert("Dynamic content load failed.");
                    }
                });
            });
            //$("#closebtn").on('click',function(){
            //    $('#myModal').modal('hide');

            $("#closbtn").click(function () {
                $('#myModal').modal('hide');
            });
        });

        </script>

    }*@
